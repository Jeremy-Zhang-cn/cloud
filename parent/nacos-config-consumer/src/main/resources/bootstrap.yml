server:
  port: ${PORT:10024}

spring:
  application:
    name: nacos-config-consumer
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 172.16.30.33:8850
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 172.16.30.33:8850